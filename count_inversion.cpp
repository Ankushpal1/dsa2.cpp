#include<iostream>
#include<vector>

using namespace std;

int countinversion(int start , int end, mid ){
    int i= start;
    int j =end;
    int mid = (start + (end-start));

    mergesort(start, end, mid)


}

int main(){
    int ans=0;
    count ans= countinversion(arr, start)
}